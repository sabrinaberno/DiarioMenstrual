package controller;

import java.awt.event.ActionEvent;

import javax.swing.JButton;

import Database.DB;
import model.Pessoa;
import view.Home;


public class HomeController {
	
	/*private final Home view;

	public HomeController(Home view) {
		super();
		this.view = view;
	}

	public void executarBotao(ActionEvent e) {
		JButton botaoPressionado = (JButton) e.getSource();

		if (botaoPressionado == view.getCancelar()) {
			this.view.dispose();
		} else {
				DB.getPessoas().add(this.createModel());
				System.out.println(DB.getPessoas());			
		} 
	}

private Pessoa createModel() {
	String login = this.view.getFieldLogin().getText();
	String senha = this.view.getFieldSenha().getText();
	
	return new Pessoa(login, senha);
}


	public Home getView() {
		return view;
	} */
}

